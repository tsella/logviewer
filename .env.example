PORT=3000
# Comma-separated list of allowed daemon names
ALLOWED_DAEMONS=nginx

# Optional: Maximum number of log lines to keep in memory per connection
MAX_LOG_LINES=1000

# Optional: SSE heartbeat interval in milliseconds
HEARTBEAT_INTERVAL=30000
